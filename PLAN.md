# 鬥獸棋（`pg-jungle`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-jungle.yaml`

## 1. 一句話

7×9 華語圈懷舊鬥獸棋：獸階吃子、鼠吃象、陷阱任意吃、僅鼠下水、獅虎跳河，攻入對方獸穴或吃光對手即勝，人機對戰；民間常見玩法的原創 UI 實作小品，非任一商業作品復刻。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-jungle` / `game` / `懷舊` |
| status | `listed` |
| 模式 | 單人 vs AI（玩家紅方在下、先行）；單局制 |
| 棋盤 | 7×9（`COLS=7`,`ROWS=9`），Canvas 邏輯 480×640 |
| 勝負 | 攻入敵方獸穴／吃光對方／AI 無棋可走 → 勝；己方被吃光 → 敗 |
| 對手 AI | 單一難度：貪心評分＋微擾隨機（延遲 380–640ms） |
| 素材 | 全程式繪製（棋盤／獸棋 token）＋WebAudio 合成音效（master 0.18） |
| 持久化 | **無**（無 KV、無 localStorage；音效開關僅記憶體） |
| 交付形 | ESM 純 JS＋Canvas；無 build；**現無測試** |

## 3. 完整規則（現行實作）

### 3.1 棋子與獸階（`game.js`）

- 八獸階級 `RANK`：象 8＞獅 7＞虎 6＞豹 5＞狼 4＞狗 3＞貓 2＞鼠 1。
- 吃子表 `canCapture(a,b,defenderInTrap)`：守方在攻方陷阱 → **任何獸可吃任何獸**；鼠可吃象；象不可吃鼠（即使階數高）；其餘按 `RANK[a] ≥ RANK[b]`。
- 初始佈陣（AI 在上、玩家在下）：AI＝獅(0,0) 虎(0,6) 狗(1,1) 貓(1,5) 鼠(2,0) 豹(2,2) 狼(2,4) 象(2,6)；玩家鏡像＝獅(8,6) 虎(8,0) 狗(7,5) 貓(7,1) 鼠(6,6) 豹(6,4) 狼(6,2) 象(6,0)。

### 3.2 地形

- 獸穴：`(0,3)` AI 方、`(8,3)` 玩家方——**禁入自家穴**；踏入敵穴立即分勝負。
- 陷阱六格：`(0,2)(0,4)(1,3)` 屬 AI、`(8,2)(8,4)(7,3)` 屬玩家（`trapOwner` 按 r≤1/r≥7 歸屬）。守方在**攻方的**陷阱中即失去階級保護（可被任意吃）。
- 河界兩湖：列 3–5 × 行 {1,2} 與 {4,5} 共 12 格水。**僅鼠能入水**；其他獸不得踏水。

### 3.3 移動與特殊規則

- 一般移動：上下左右一格（`dr+dc === 1`）；目標有己方獸拒絕；目標有敵獸依吃子表判定。
- **獅虎跳河**：同行/同列直線跨越整片湖——垂直跳須跨距恰 4 格且不在中線 c=3，水平跳限列 3–5 且跨距恰 3 格；**路徑各水格若有任一方之鼠擋道即不可跳**；起訖都必須是陸地；落點有敵獸照常判吃（含陷阱加權）。
- **鼠與水**：鼠從水中或跳入水中落子時**不得吃象**（`fromWater || toWater` 即禁，雙向皆然）——水陸互動隔離；象吃鼠本就被吃子表封死（例外僅剩鼠陷己方陷阱）。
- 回合流：點己方獸選取（再點同獸取消）→ 點目標格嘗試走子；非法則清除選取並播 deny 音。玩家先手；玩家走完輪 AI。

### 3.4 勝負條件

- 走子落入敵穴（玩家 r=0/c=3、AI r=8/c=3）→ 即刻 `win`／`lose`。
- `checkEnd` 盤面清點：AI 全滅 →「消滅對方」勝；玩家全滅 → 敗。
- AI 端無合法著手 → 直接判「對方無棋可走，你贏了」。**已知缺口：玩家端無棋可走未偵測**（玩家會卡死無法行動）——見 §9。
- AI 走完後若非因吃子結束，訊息重設為「輪到你了」。

### 3.5 AI 行為（`listMoves` + `aiMove`）

- 全盤枚舉所有己方合法著手並打分：基礎 1；吃子 20＋守方階級；入獸穴 1000；向敵營推進每格 +0.5（AI 向下、玩家向上）。
- 取最高分 −2 以內為候選群，於前 3 名內隨機挑一（微擾避免呆板重複）。
- 出手節奏：app 排程 `380 + random×260` ms，其間 `aiThinking=true` 封鎖玩家輸入。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 點己方獸棋 | 選取（藍圈）；再點取消 |
| 點高亮格 | 移動／吃子（黃圈＝該子全部合法目的地，由 `highlights()` 列出） |
| 新局 | 重置（非破壞性，不需確認） |
| 音效開/關 | 靜音（僅記憶體） |

- Canvas 480×640：綠底金框、地形配色（水藍漸層／穴深灰書「穴」字／阱棕框書「阱」字）、獸棋為圓形 token——玩家紅系、AI 黑灰系，中央小色盤表獸種＋宋體單字標籤＋雙眼。
- 圖例區顯示回合狀態與雙方剩餘數；HUD 另有 你/電腦 剩餘比。Mobile-first 直向；禁原生對話框。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| （無） | — | 本 repo 完全未用 localStorage／`/api/kv`／KV binding；戰績與偏好皆不保存 |

- 多人協定小節：**未實作（無 Invite/session 支援）**。型錄條目無 protocols；`functions.js` 僅 stub（回 `{ok,name,path}`）。未來接入請仿 `pg-gomoku` 的 protocol.js＋functions 權威模式。

## 6. 美術／音效／署名

- 無外部素材（repo 無 `assets/` 目錄）：棋盤、地形、獸棋全 Canvas 程序繪製（`sprites.js`）；字型用 Songti TC serif／system-ui。注意：**本 repo 尚無 ATTRIBUTION.md**——目前素材皆自繪故無義務，但新增第三方素材時須補立此檔（CC0 也署名）並同步 `sam-manifest.json` files。
- 音效全 WebAudio 合成（`audio.js`，master 0.18，square/triangle/sawtooth 振盪器）：選子 640Hz 短音、移動 300Hz、吃子 180+400Hz 低吼、非法 100Hz、勝利 380Hz 起 ×1.15ⁿ 六連上行音階、失敗下行雙音。AudioContext 手勢解鎖（body once ＋每次操作 unlock）。

## 7. 測試

**現況：零測試**（repo 無任何 `*.test.js`，亦無 vitest config）——規則核心 `game.js` 卻是純函式、完全可測，屬明顯欠帳。

最小必測建議（新增 `game.test.js`，`npx --yes vitest run`）：

1. `canCapture` 表：同階互吃、鼠吃象、象不吃鼠、陷阱任意吃三態。
2. `isLegalMove`：一步正交、拒入自家穴、非鼠禁入水。
3. 跳河幾何：垂直跨 4／水平跨 3 成功；跨距錯誤、路徑有鼠、起訖在水皆拒。
4. 陷阱歸屬：敵獸在我阱可被鼠吃；我獸在敵阱不受保護反向成立。
5. 勝負：入敵穴即勝、入自家穴被拒、清光對手勝、AI 無棋判勝。
6. `aiMove` 返回合法著手且有提子時優先吃子。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS（ESM)；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；狀態一律 status 列。
3. Mobile-first；主操作不可 hover-only（點選＋高亮已符合）。
4. 若新增戰績/進度持久化，唯一權威＝宿主 `/api/kv/{key}`；禁裸 localStorage 當權威。
5. 不自行載入 `sdk.js`；本作目前不依賴 `window.PG`，靜態伺服即可玩。
6. 改動可執行邏輯前先寫失敗測試（TDD）——本 repo 測試基線為零，第一個邏輯改動就該把 §7 最小測試補上。
7. 檔案清單變動須同步 `sam-manifest.json`。
8. 規則核心維持在 `game.js` 純函式（DOM-free）；`sprites.js` 只吃 game 狀態繪製，不得反過來寫入規則。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：強化決策深度與懷舊手感，不改變「階級吃子＋地形博弈」的核心認同。

**高優先**

1. **AI 難度三段**（休閒＝現行貪心／標準＝2-ply 極小極大／兇猛＝3-ply＋陷阱意識）：`listMoves` 已產生完整著手表，包一層 minimax 成本低；現行貪心會無腦送子給更高階獸，熟手贏得太容易。
2. **補零測試基線**：§7 六組測試落地後才值得動規則；否則任何吃子表或跳河改動都在裸奔。
3. **戰績持久化**：`/api/kv/pg-jungle-stats` 存總場/勝場/連勝，HUD 顯示——現行關頁即忘，毫無重玩累積感。

**中優先**

4. **玩家無棋可走判定**：`checkEnd` 只查 AI 端；補玩家端無合法著手 → 判負（或跳過回合），消滅卡死死角。
5. **吃子提示**：選中獸時在其可吃的目標上加警示描邊（吃子表新手記不住，特別是鼠象剋制）。
6. **同機熱座**：上下翻轉棋盤給第二位玩家（AI 抽換成第二輸入源），社交玩法零硬體成本。

**低優先**

7. **走子動畫**：token 平滑位移＋吃子淡出，取代現行瞬移重繪（rAF 迴圈已在跑，插值即可）。
8. **陷阱歸屬視覺化**：陷阱格以紅/藍細框標示敵我歸屬——規則常被忽略，「敵獸在你的陷阱可被任意吃」靠圖例文字難以內化。
